name: build non-obviousness ranking (Railway-side compute)

on:
  workflow_dispatch:

concurrency:
  group: build-non-obviousness
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SHELLNET_JOB_URL: ${{ vars.SHELLNET_JOB_URL }}
      SHELLNET_JOB_TOKEN: ${{ secrets.SHELLNET_JOB_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
      - run: uv sync --frozen --no-dev
      - run: curl -fsS "$SHELLNET_JOB_URL/healthz"
      - name: Trigger build_non_obviousness_score
        run: |
          set -euo pipefail
          curl -fsS -X POST \
            -H "Authorization: Bearer $SHELLNET_JOB_TOKEN" \
            "$SHELLNET_JOB_URL/run-script?name=build_non_obviousness_score"
          echo
      - name: Poll until complete
        run: |
          set -euo pipefail
          stage="script_build_non_obviousness_score"
          done=false
          for _ in $(seq 1 180); do
            sleep 5
            s=$(curl -fsS -H "Authorization: Bearer $SHELLNET_JOB_TOKEN" \
                "$SHELLNET_JOB_URL/status" \
                | python -c "import sys,json; d=json.load(sys.stdin); print(d['stages'].get('$stage',{}).get('status','?'))")
            echo "  $stage: $s"
            case "$s" in
              completed) done=true; break ;;
              failed)
                curl -fsS -H "Authorization: Bearer $SHELLNET_JOB_TOKEN" \
                  "$SHELLNET_JOB_URL/logs/$stage?tail=120" || true
                exit 1
                ;;
            esac
          done
          if [ "$done" != "true" ]; then
            echo "::error::poll loop timed out"
            exit 1
          fi
      - name: Download artifacts
        run: |
          set -euo pipefail
          mkdir -p data/processed docs/reports/data
          for p in processed/non_obviousness_scores.parquet \
                   processed/non_obviousness_summary.json; do
            curl -fsS -H "Authorization: Bearer $SHELLNET_JOB_TOKEN" \
              "$SHELLNET_JOB_URL/download?path=$p" -o "data/$p"
            cp "data/$p" "docs/reports/data/$(basename $p)"
          done
      - name: Render markdown
        run: |
          set -euo pipefail
          uv run python scripts/render_non_obviousness.py \
            --parquet docs/reports/data/non_obviousness_scores.parquet \
            --summary docs/reports/data/non_obviousness_summary.json \
            --out docs/reports/non_obviousness_ranking.md
      - name: Commit + push to main
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/reports/non_obviousness_ranking.md \
                  docs/reports/data/non_obviousness_scores.parquet \
                  docs/reports/data/non_obviousness_summary.json
          if git diff --cached --quiet; then
            echo "No changes."
            exit 0
          fi
          git commit -m "report: refresh non-obviousness ranking (auto from build-non-obviousness.yml)"
          git pull --rebase origin main
          git push origin HEAD:main
