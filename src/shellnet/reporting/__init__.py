"""Generated artefacts: coverage reports, top-N tables, evaluation summaries.

Everything in here writes plain-text or JSON outputs under
``reports/generated/`` (git-ignored) or ``reports/`` (committable, if the
caller chooses). Nothing here should hit the network.
"""
