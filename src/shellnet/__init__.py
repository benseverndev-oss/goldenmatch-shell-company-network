"""shellnet — reconcile shell-company / offshore-entity networks across public datasets.

This package is the engine behind the GoldenMatch case study in this repo.
Modules are intentionally small and source-agnostic; everything that reads
from a specific data provider lives under :mod:`shellnet.sources`.
"""

__version__ = "0.1.0"
